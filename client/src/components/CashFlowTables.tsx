import { SolarInputs, SolarResults } from \"@/lib/calculator\";\nimport { formatCurrency, formatNumberWithCommas } from \"@/lib/formatters\";\nimport { Card, CardContent, CardHeader, CardTitle } from \"@/components/ui/card\";\nimport { Tabs, TabsContent, TabsList, TabsTrigger } from \"@/components/ui/tabs\";\n\ninterface CashFlowTablesProps {\n  inputs: SolarInputs;\n  results: SolarResults;\n}\n\nexport default function CashFlowTables({ inputs, results }: CashFlowTablesProps) {\n  const { cashFlows, stakeholders } = results;\n\n  // Calculate discounted cash flows\n  const discountFactor = (year: number) => 1 / Math.pow(1 + inputs.discountRate, year);\n\n  return (\n    <Tabs defaultValue=\"operator\" className=\"w-full\">\n      <TabsList className=\"grid w-full grid-cols-4\">\n        <TabsTrigger value=\"operator\">Operator</TabsTrigger>\n        <TabsTrigger value=\"offtaker\">Offtaker</TabsTrigger>\n        <TabsTrigger value=\"landowner\">Landowner</TabsTrigger>\n        <TabsTrigger value=\"developer\">Developer</TabsTrigger>\n      </TabsList>\n\n      {/* Operator Cash Flow */}\n      <TabsContent value=\"operator\" className=\"space-y-4\">\n        <Card>\n          <CardHeader>\n            <CardTitle>Operator Annual Cash Flow</CardTitle>\n          </CardHeader>\n          <CardContent>\n            <div className=\"overflow-x-auto\">\n              <table className=\"w-full text-sm\">\n                <thead>\n                  <tr className=\"border-b-2 border-navy-900 bg-navy-900 text-white\">\n                    <th className=\"px-4 py-2 text-left\">Year</th>\n                    <th className=\"px-4 py-2 text-right\">Revenue (£)</th>\n                    <th className=\"px-4 py-2 text-right\">OPEX (£)</th>\n                    <th className=\"px-4 py-2 text-right\">Net CF (£)</th>\n                    <th className=\"px-4 py-2 text-right\">Discount Factor</th>\n                    <th className=\"px-4 py-2 text-right\">Discounted CF (£)</th>\n                    <th className=\"px-4 py-2 text-right\">Cumulative DCF (£)</th>\n                  </tr>\n                </thead>\n                <tbody>\n                  {cashFlows.map((cf, index) => {\n                    const df = discountFactor(index);\n                    const dcf = cf.netCashFlow * df;\n                    const cumulativeDCF = cashFlows\n                      .slice(0, index + 1)\n                      .reduce((sum, c) => sum + c.netCashFlow * discountFactor(cashFlows.indexOf(c)), 0);\n\n                    return (\n                      <tr key={index} className=\"border-b border-gray-200 hover:bg-gray-50\">\n                        <td className=\"px-4 py-2 font-semibold\">{index}</td>\n                        <td className=\"px-4 py-2 text-right\">{formatCurrency(cf.revenue)}</td>\n                        <td className=\"px-4 py-2 text-right\">{formatCurrency(cf.opex)}</td>\n                        <td className=\"px-4 py-2 text-right font-semibold\">{formatCurrency(cf.netCashFlow)}</td>\n                        <td className=\"px-4 py-2 text-right text-gray-600\">{df.toFixed(4)}</td>\n                        <td className=\"px-4 py-2 text-right font-semibold\">{formatCurrency(dcf)}</td>\n                        <td className=\"px-4 py-2 text-right font-bold text-green-600\">{formatCurrency(cumulativeDCF)}</td>\n                      </tr>\n                    );\n                  })}\n                </tbody>\n              </table>\n            </div>\n            <div className=\"mt-4 space-y-2 border-t pt-4 text-sm\">\n              <div className=\"flex justify-between\">\n                <span>Total NPV:</span>\n                <span className=\"font-bold\">{formatCurrency(results.summary.totalDiscountedCashFlow)}</span>\n              </div>\n              <div className=\"flex justify-between\">\n                <span>IRR (Unlevered):</span>\n                <span className=\"font-bold\">{((results.summary.irr || 0) * 100).toFixed(2)}%</span>\n              </div>\n            </div>\n          </CardContent>\n        </Card>\n      </TabsContent>\n\n      {/* Offtaker Savings */}\n      <TabsContent value=\"offtaker\" className=\"space-y-4\">\n        <Card>\n          <CardHeader>\n            <CardTitle>Offtaker Annual Savings</CardTitle>\n          </CardHeader>\n          <CardContent>\n            <div className=\"overflow-x-auto\">\n              <table className=\"w-full text-sm\">\n                <thead>\n                  <tr className=\"border-b-2 border-green-700 bg-green-700 text-white\">\n                    <th className=\"px-4 py-2 text-left\">Year</th>\n                    <th className=\"px-4 py-2 text-right\">Annual Savings (£)</th>\n                    <th className=\"px-4 py-2 text-right\">Discount Factor</th>\n                    <th className=\"px-4 py-2 text-right\">Discounted Savings (£)</th>\n                    <th className=\"px-4 py-2 text-right\">Cumulative Discounted (£)</th>\n                  </tr>\n                </thead>\n                <tbody>\n                  {cashFlows.map((cf, index) => {\n                    const annualSavings = cf.revenue * 0.1; // Placeholder calculation\n                    const df = discountFactor(index);\n                    const discountedSavings = annualSavings * df;\n                    const cumulativeDiscounted = cashFlows\n                      .slice(0, index + 1)\n                      .reduce(\n                        (sum, c, i) => sum + (c.revenue * 0.1) * discountFactor(i),\n                        0\n                      );\n\n                    return (\n                      <tr key={index} className=\"border-b border-gray-200 hover:bg-gray-50\">\n                        <td className=\"px-4 py-2 font-semibold\">{index}</td>\n                        <td className=\"px-4 py-2 text-right\">{formatCurrency(annualSavings)}</td>\n                        <td className=\"px-4 py-2 text-right text-gray-600\">{df.toFixed(4)}</td>\n                        <td className=\"px-4 py-2 text-right font-semibold\">{formatCurrency(discountedSavings)}</td>\n                        <td className=\"px-4 py-2 text-right font-bold text-green-600\">{formatCurrency(cumulativeDiscounted)}</td>\n                      </tr>\n                    );\n                  })}\n                </tbody>\n              </table>\n            </div>\n            <div className=\"mt-4 border-t pt-4 text-sm\">\n              <div className=\"flex justify-between font-bold\">\n                <span>Total NPV (Offtaker):</span>\n                <span>{formatCurrency(results.summary.totalSavings)}</span>\n              </div>\n            </div>\n          </CardContent>\n        </Card>\n      </TabsContent>\n\n      {/* Landowner Income */}\n      <TabsContent value=\"landowner\" className=\"space-y-4\">\n        <Card>\n          <CardHeader>\n            <CardTitle>Landowner Annual Rental Income</CardTitle>\n          </CardHeader>\n          <CardContent>\n            <div className=\"overflow-x-auto\">\n              <table className=\"w-full text-sm\">\n                <thead>\n                  <tr className=\"border-b-2 border-yellow-600 bg-yellow-600 text-white\">\n                    <th className=\"px-4 py-2 text-left\">Year</th>\n                    <th className=\"px-4 py-2 text-right\">Annual Income (£)</th>\n                    <th className=\"px-4 py-2 text-right\">Discount Factor</th>\n                    <th className=\"px-4 py-2 text-right\">Discounted Income (£)</th>\n                    <th className=\"px-4 py-2 text-right\">Cumulative Discounted (£)</th>\n                  </tr>\n                </thead>\n                <tbody>\n                  {cashFlows.map((cf, index) => {\n                    const annualIncome = inputs.landOptionCostPerMWYear * inputs.mw;\n                    const df = discountFactor(index);\n                    const discountedIncome = annualIncome * df;\n                    const cumulativeDiscounted = cashFlows\n                      .slice(0, index + 1)\n                      .reduce((sum) => sum + annualIncome * df, 0);\n\n                    return (\n                      <tr key={index} className=\"border-b border-gray-200 hover:bg-gray-50\">\n                        <td className=\"px-4 py-2 font-semibold\">{index}</td>\n                        <td className=\"px-4 py-2 text-right\">{formatCurrency(annualIncome)}</td>\n                        <td className=\"px-4 py-2 text-right text-gray-600\">{df.toFixed(4)}</td>\n                        <td className=\"px-4 py-2 text-right font-semibold\">{formatCurrency(discountedIncome)}</td>\n                        <td className=\"px-4 py-2 text-right font-bold text-yellow-700\">{formatCurrency(cumulativeDiscounted)}</td>\n                      </tr>\n                    );\n                  })}\n                </tbody>\n              </table>\n            </div>\n            <div className=\"mt-4 border-t pt-4 text-sm\">\n              <div className=\"flex justify-between font-bold\">\n                <span>Total NPV (Landowner):</span>\n                <span>{formatCurrency(results.summary.totalLandOptionIncome)}</span>\n              </div>\n            </div>\n          </CardContent>\n        </Card>\n      </TabsContent>\n\n      {/* Developer Premium */}\n      <TabsContent value=\"developer\" className=\"space-y-4\">\n        <Card>\n          <CardHeader>\n            <CardTitle>Developer Premium</CardTitle>\n          </CardHeader>\n          <CardContent>\n            <div className=\"space-y-4\">\n              <div className=\"rounded-lg bg-navy-900 p-6 text-white\">\n                <p className=\"text-sm text-gray-300\">Development Premium (Year 0)</p>\n                <p className=\"text-3xl font-bold\">{formatCurrency(results.summary.totalDeveloperPremium)}</p>\n              </div>\n              <div className=\"grid grid-cols-2 gap-4\">\n                <div className=\"rounded-lg border border-gray-200 p-4\">\n                  <p className=\"text-xs text-gray-600\">Premium per MW</p>\n                  <p className=\"text-lg font-bold\">{formatCurrency(inputs.developmentPremiumPerMW)}</p>\n                </div>\n                <div className=\"rounded-lg border border-gray-200 p-4\">\n                  <p className=\"text-xs text-gray-600\">Total MW</p>\n                  <p className=\"text-lg font-bold\">{formatNumberWithCommas(inputs.mw)}</p>\n                </div>\n              </div>\n            </div>\n          </CardContent>\n        </Card>\n      </TabsContent>\n    </Tabs>\n  );\n}\n
