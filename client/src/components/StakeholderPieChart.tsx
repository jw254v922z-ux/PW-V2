import { PieChart, Pie, Cell, Legend, Tooltip, ResponsiveContainer } from \"recharts\";\n\ninterface StakeholderPieChartProps {\n  operatorNPV: number;\n  offtakerSavings: number;\n  landownerIncome: number;\n  developerPremium: number;\n  totalValue: number;\n}\n\nexport default function StakeholderPieChart({\n  operatorNPV,\n  offtakerSavings,\n  landownerIncome,\n  developerPremium,\n  totalValue,\n}: StakeholderPieChartProps) {\n  const data = [\n    {\n      name: \"Operator\",\n      value: operatorNPV,\n      color: \"#808080\",\n    },\n    {\n      name: \"Offtaker\",\n      value: offtakerSavings,\n      color: \"#2D8659\",\n    },\n    {\n      name: \"Landowner\",\n      value: landownerIncome,\n      color: \"#FFD700\",\n    },\n    {\n      name: \"Developer\",\n      value: developerPremium,\n      color: \"#001F3F\",\n    },\n  ].filter((item) => item.value > 0);\n\n  return (\n    <div className=\"w-full\">\n      <ResponsiveContainer width=\"100%\" height={300}>\n        <PieChart>\n          <Pie\n            data={data}\n            cx=\"50%\"\n            cy=\"50%\"\n            labelLine={false}\n            label={({ name, value, color }) => (\n              <text\n                x={0}\n                y={0}\n                fill={color}\n                textAnchor=\"middle\"\n                dominantBaseline=\"central\"\n                className=\"font-bold text-xs\"\n              >\n                {name} ({((value / totalValue) * 100).toFixed(1)}%)\n              </text>\n            )}\n            outerRadius={80}\n            fill=\"#8884d8\"\n            dataKey=\"value\"\n          >\n            {data.map((entry, index) => (\n              <Cell key={`cell-${index}`} fill={entry.color} />\n            ))}\n          </Pie>\n          <Tooltip\n            formatter={(value: number) => `£${(value / 1000000).toFixed(2)}M`}\n            contentStyle={{\n              backgroundColor: \"#fff\",\n              border: \"1px solid #ccc\",\n              borderRadius: \"4px\",\n            }}\n          />\n        </PieChart>\n      </ResponsiveContainer>\n    </div>\n  );\n}\n
